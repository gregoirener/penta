# Where you put penta-<sha>.img.xz after flashing.
#
# Empty on purpose, and FAT32 on purpose: this is the partition that appears in
# Finder and in Explorer when the stick is plugged in, so getting the payload
# onto it is drag-and-drop rather than a dd incantation.
#
# 6 GiB of zeros costs a few hundred KB in the compressed artifact, so this
# partition is close to free — and it leaves room for a future image well
# larger than today's 2 GB one.
#
# Last in filename order so it is the partition at the end of the device, which
# is what lets `penta-install` grow it to fill a bigger stick if it ever needs
# to.
[Partition]
# Microsoft basic data, NOT linux-generic. The GPT type GUID is what macOS and
# Windows use to decide whether a partition is theirs to mount — a FAT32
# filesystem tagged "Linux filesystem" can be silently ignored by Finder, and
# then the one manual step in this whole procedure (drag the image onto the
# volume) is impossible. This is the GUID every USB stick on earth uses.
Type=ebd0a0a2-b9e5-4433-87c0-68b6b72699c7
Format=vfat
# 9 characters, and that is not an accident: a FAT32 volume label is capped at
# ELEVEN. "PENTA_PAYLOAD" is thirteen, so mkfs.vfat silently truncated it to
# PENTA_PAYLO — the GPT partition name stayed correct, the filesystem label did
# not, and everything that looked the volume up by name (`blkid -L`, the mount
# point under /Volumes) missed it. Silent truncation, three different symptoms.
Label=PENTA_IMG
SizeMinBytes=6G
SizeMaxBytes=6G
