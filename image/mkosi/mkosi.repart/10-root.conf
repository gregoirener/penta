# Root. Fixed at build time; it never needs to grow because games live on the
# data partition. btrfs so system snapshots ("restore console") are possible.
[Partition]
Type=root-x86-64
Format=btrfs
Label=PENTA_SYS
SizeMinBytes=24G
SizeMaxBytes=24G
