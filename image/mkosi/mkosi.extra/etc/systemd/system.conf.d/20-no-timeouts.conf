[Manager]
# No start/stop timeouts anywhere. A console has no operator to rescue a unit
# that gives up at 90 seconds, and every timeout so far has turned a slow step
# into a failed boot rather than a slow boot.
DefaultTimeoutStartSec=infinity
DefaultTimeoutStopSec=45s
DefaultDeviceTimeoutSec=infinity
