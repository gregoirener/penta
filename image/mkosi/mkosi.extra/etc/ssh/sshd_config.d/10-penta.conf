# Key-only root access. `penta` is a service account with no shell, so the dev
# loop (tools/devsync.sh) comes in here. There is no password to guess.
PermitRootLogin prohibit-password
PasswordAuthentication no
KbdInteractiveAuthentication no
